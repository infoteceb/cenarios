{
  "name": "dash-cenarios",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8000",
    "clean": "rimraf dist",
    "build:prod": "npm run clean && npm run build",
    "serve": "npm run build && http-server dist -p 3000 -o",
    "start": "http-server dist -p 3000 -o"
  },
  "devDependencies": {
    "glob": "^10.5.0",
    "rimraf": "^5.0.5",
    "vite": "^5.1.0"
  }
}
