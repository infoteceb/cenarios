{
  "name": "dash-cenarios",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8000",
    "clean": "rimraf dist",
    "build:prod": "npm run clean && npm run build"
  },
  "devDependencies": {
    "glob": "^10.3.10",
    "rimraf": "^5.0.5",
    "vite": "^5.1.0"
  }
}
